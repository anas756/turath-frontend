import { createSlice } from '@reduxjs/toolkit';

const initialState = {};
export const BookSlice = createSlice({
  name: 'book',
  initialState,
  reducers: {},
    extraReducers: (nuilder) => {
      
  },
});
export const { } = BookSlice.actions;